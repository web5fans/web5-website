---
const features = [
  {
    title: 'Decentralized Identifiers',
    description: 'Self-sovereign identity. Users own their identifiers, not companies.',
    icon: '🆔',
    color: 'bg-blue-500/10 text-blue-400'
  },
  {
    title: 'Verifiable Credentials',
    description: 'Tamper-evident claims. Prove things about yourself without revealing everything.',
    icon: '📜',
    color: 'bg-purple-500/10 text-purple-400'
  },
  {
    title: 'Decentralized Web Nodes',
    description: 'Personal data stores. Users hold their data and grant permission to apps.',
    icon: '📦',
    color: 'bg-cyan-500/10 text-cyan-400'
  },
];
---

<section class="py-24 bg-slate-950 sm:py-32">
  <div class="mx-auto max-w-7xl px-6 lg:px-8">
    <div class="mx-auto max-w-2xl lg:text-center">
      <h2 class="text-base font-semibold leading-7 text-cyan-400">Core Technologies</h2>
      <p class="mt-2 text-3xl font-bold tracking-tight text-white sm:text-4xl">Everything you need to build Web5</p>
      <p class="mt-6 text-lg leading-8 text-slate-400">
        Web5 consists of three main pillars that work together to enable a new class of decentralized applications.
      </p>
    </div>
    <div class="mx-auto mt-16 max-w-2xl sm:mt-20 lg:mt-24 lg:max-w-none">
      <dl class="grid max-w-xl grid-cols-1 gap-x-8 gap-y-16 lg:max-w-none lg:grid-cols-3">
        {features.map((feature) => (
          <div class="flex flex-col bg-slate-900/50 rounded-2xl p-8 border border-slate-800 hover:border-slate-700 transition-all duration-300 hover:-translate-y-1">
            <dt class="flex items-center gap-x-3 text-base font-semibold leading-7 text-white">
              <div class={`h-10 w-10 flex items-center justify-center rounded-lg ${feature.color}`}>
                <span class="text-xl">{feature.icon}</span>
              </div>
              {feature.title}
            </dt>
            <dd class="mt-4 flex flex-auto flex-col text-base leading-7 text-slate-400">
              <p class="flex-auto">{feature.description}</p>
            </dd>
          </div>
        ))}
      </dl>
    </div>
  </div>
</section>
